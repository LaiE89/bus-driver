using UnityEngine;

// Gameplay keys, statics in the same style as ControlsMenu.switchCameraKey so the
// controls menu can rebind them later
public static class GameKeys {
    public static KeyCode interact = KeyCode.E;
    public static KeyCode doors = KeyCode.F;
}
